# TODO

## Planned Features

### Re-orderable UI elements
Allow re-ordering of top-level task tracker elements (task groups, new group input, tabs, progress bar) via a layout/order setting.

### Image support in tasks
Support attaching or embedding images within tasks (inline preview, lightbox, or link-based).

### Inline edit mode (setting)
Move the edit modal to an inline editing experience when clicking a task element directly. Gated behind a setting under Advanced, defaulted off.

### Next TODOs click-to-scroll
Clicking an item in the Next TODOs section scrolls to and highlights the corresponding task under its group. Gated behind a setting. Include a secondary setting to change the behavior from click-on-item to a dedicated button to the right of the edit icon (only shown on tasks in the Next TODOs list, not on the real task element under the group).
